#Calulate the factorial
def factorial(num):
  if num == 1:
    print("1 * 1 = 1")
  else:
    result = 1
    expression = "1"
    for i in range(2, num + 1):
      result *= i
      expression += f" * {i}"
    print(f"{expression} = {result}")
num = int(input("Enter the number:"))
factorial(num)
